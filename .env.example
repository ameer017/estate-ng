# Local (docker compose up -d) — port 5434 so RailNG (5432) and MediVault (5433) can keep theirs
DATABASE_URL="postgresql://estateng:estateng@localhost:5434/estateng"

# Netlify: Neon / Supabase connection string, sslmode=require
# DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require"

AUTH_SECRET="replace-with-a-long-random-string"
AUTH_URL="http://localhost:3000"
